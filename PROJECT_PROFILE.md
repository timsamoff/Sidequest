# Project Profile: Sidequest

Generated by Sentinel: Project Discovery (v1.0). This is a fresh run — no prior `PROJECT_PROFILE.md` existed.

## Agent and environment

- AI coding agent in use: Claude Code
- Persistent project-context file name: `CLAUDE.md` (not yet present at project root — see below)
- Permission/configuration mechanism: `.claude/settings.json` (not yet present in this project)

## Project name

**Sidequest** (confirmed by user; matches the README title and the public repo name `timsamoff/Sidequest`; `package.json`'s `name` field uses the lowercase npm-style `sidequest`).

## Tech stack / primary language(s)

- Single-file vanilla JavaScript, HTML, and CSS. No framework, no build step, no bundler.
- `package.json` exists only to declare the test setup: `devDependencies` has `jsdom` (^30.1.0), `engines.node` requires >=22, and the one script is `test: node tests/run.js`.
- No backend, no server, no database. All persistence is browser `localStorage`.

## Application surfaces

This is a single-surface project, not a monorepo:

- **`index.html`** — the entire application (~2026 lines). One self-contained file: inline CSS, inline JavaScript, inline SVG logo. This is the source of truth and the thing that gets copied into Claude's artifact sandbox or hosted via GitHub Pages.
- **`assets/`** — repo-only icon files (favicon, apple-touch-icon, PNG/SVG app icons). Explicitly *not* required at runtime; the app must work without them since they don't travel with the artifact.
- **`tests/`** — `run.js` (a tiny test runner, 16 lines) and `template.test.js` (159 lines) using jsdom to simulate a browser for the single-file app.

No independent packages or secondary apps. Everything shares the one `index.html` file.

## Design/token system file

Design tokens exist as CSS custom properties in a `:root` block inside `index.html` itself (starting around line 12), not a separate token file — consistent with the single-file constraint. Key tokens include `--bg`, `--planned`, `--actual`, `--burn`, `--focus`, `--late`, with a dark-mode override block under `:root:not([data-theme="light"])` and `:root[data-theme="dark"]`. There is no `design-tokens.json`, Tailwind config, or platform color-asset catalog — none is expected given the architecture.

## Recurring unit of work

Per user confirmation, this project has three recurring shapes of change rather than one dominant unit:

1. **A new view/page** — e.g. adding something alongside Today, Schedule, Projects, Timeline, Archive, Help, Settings, Search, or a new pinned page. The largest recurring structural addition.
2. **A new state field** — e.g. adding a field like `pins`, `pset`, or `pest` to the saved state object, which must get a default in `normalize()` so older saved data keeps loading.
3. **A new UI feature/control** — e.g. a new button, menu entry, or interactive widget inside an existing view.

The Integration Audit should treat all three as valid touchpoint categories rather than assuming a single canonical unit.

## VCS status

- Git repository: yes.
- Commits: 2 (`bd33665` "Initial commit", `efdf8f8` "v1.0.0").
- Contributors: 1 (Tim Samoff).
- Commit-message convention: both existing messages are short, single-line, no Conventional Commits prefix, no body. Matches the user's own stated preference (see CLAUDE.md global instructions: single-line summaries by default, no co-author notes). Too small a sample to call it a strictly "established" convention from git history alone, but it's consistent with the user's documented default working style.
- Remote: `origin` → `https://github.com/timsamoff/Sidequest.git`.
- No tags.

## Version/release status

- `package.json` version: `1.0.0`.
- No git tags/releases recorded yet, but the version string and the second commit message (`v1.0.0`) indicate this reads as an established v1.0 release, not pre-release/experimental — it is described as a public repo meant to be shared/published as a Claude artifact.

## Existing automation

- No CI config (no `.github/` directory).
- No git hooks beyond the default Git sample hooks (`.git/hooks/*.sample`, all inactive).
- One npm script: `test` → `node tests/run.js`.
- No existing gate-check or quality-enforcement system of any kind. This will be a from-scratch install if the Gate-Check Implementation prompt is run.

## Existing docs and scaffolding

- `AGENTS.md`: not found.
- `CLAUDE.md` (project-level): not found. Note: a global `~/.claude/CLAUDE.md` exists for this user but is account-wide, not project-specific, and doesn't substitute for a project-level file per this suite's conventions.
- `README.md`: exists, present and current (describes app features, setup, testing, files, and credit).
- `LICENSE`: exists (not read in full per this prompt's scope).
- `.gitignore`: exists — two lines, `node_modules/` and `.DS_Store`.
- `sentinel-notes/`: not found.
- `scratch/`: not found.
- Note: the user's documented habit (per global CLAUDE.md) is normally a `production_notes/` folder with `todo.md`, kept out of git via `.gitignore` — this is the pre-1.0 naming Part 0 asks to flag when found. It was **not found** in this repo, so there is nothing to migrate, but it's worth noting as the user's usual pattern in case it appears later or the user expects it.

## Existing design documentation

None found. No `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/adr/`, or README-linked design doc. The closest equivalent to a design document is the detailed project constraints and decision log embedded in chat handoffs (e.g. the "Decisions already made" and "Purpose and constraints" sections the user has supplied directly in conversation) — these live outside the repo and are not a substitute the Design & Code Quality Audit can read directly, but are worth the user knowing this suite doesn't currently have a durable, in-repo place to check documented intent against implementation.

## Sentinel version and run type

- Sentinel version: 1.0.
- Run type: **fresh run** (no prior `PROJECT_PROFILE.md` existed).
- Not an update run — no prior gate-check hook exists, so no re-validation flag applies.

## Blank/near-empty project status

**Not blank.** This is a populated, working v1.0 application with real code (~2026-line `index.html`), a real test suite, a README, and 2 commits. Full Part 1 detection was run normally; no fields were deferred.

## Flagged for follow-up

Incidental items noticed during discovery that are out of scope for this prompt's own Part 1/Part 2 but worth surfacing once `TODO.md` exists (per the Design & Code Quality Audit's Part 0):

- **No project-level `CLAUDE.md` or `AGENTS.md` exists yet**, despite this being a project with unusually detailed, hard-edged constraints (artifact-sandbox compatibility, storage-key stability, single-file requirement) that are currently living only in ad hoc chat handoffs rather than a durable in-repo file. This project seems like a strong candidate for one, separate from whatever Sentinel's own gate-check prompt ends up producing.
- **No `sentinel-notes/` or `production_notes/` folder exists**, even though the user's normal habit (per their global instructions) is to keep exactly this kind of durable audit/findings record. Worth asking the user directly whether they want one seeded when `01`/`02` run, rather than assuming its absence means they don't want it here.
- **Commit history is very small (2 commits)** with no Conventional-Commits-style prefixes and no bodies — consistent with, but not strong independent evidence for, the user's stated global preference for terse single-line commits. The Integration Audit's Part 4 should propose matching this observed pattern and confirm with the user rather than treating 2 commits as a fully "established" convention on its own.

## How to use this

When running the Design & Code Quality Audit (`01`) or the Integration Audit (`02`), fill their `[PROJECT-SPECIFIC]` brackets from the values above rather than re-detecting or guessing fresh.

This is a fresh run against a populated codebase, so the next step is simply: **run the Design & Code Quality Audit prompt (`01`) next**, followed by the Integration Audit (`02`), then the Gate-Check Implementation prompt (`03`) once the proposals from `01` and `02` have been reviewed and approved.

Per this prompt's own closing guidance: on a genuinely fresh run, no commit-message convention has been formally established yet by this suite's own process (that happens in `02`'s Part 4), so this file is left uncommitted for now rather than proactively pushed toward a commit. If you'd like `PROJECT_PROFILE.md` committed anyway, say so and it can be done as a plain single-line commit consistent with your existing history.

{
  "name": "sidequest",
  "version": "1.0.0",
  "description": "A small, static project planner that runs in the browser.",
  "private": true,
  "scripts": {
    "test": "node tests/run.js",
    "test:a11y": "node tests/a11y.test.js",
    "sentinel:check": "node .githooks/sentinel-check.js",
    "sentinel:check:full": "node .githooks/sentinel-check.js --full",
    "build:artifact": "node scripts/build-artifact.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "jsdom": "^30.1.0",
    "playwright": "^1.63.0"
  }
}
